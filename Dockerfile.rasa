# Dockerfile.rasa
FROM rasa/rasa:3.6.2-full

USER root
WORKDIR /app

# Copy toàn bộ project
COPY . /app

EXPOSE 8080
USER 1001

# ENTRYPOINT của base image là ["rasa"]
CMD ["run","--enable-api","--cors","*","-i","0.0.0.0","-p","8080","--endpoints","endpoints.yml","--credentials","credentials.yml"]
