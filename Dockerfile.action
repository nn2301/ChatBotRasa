FROM rasa/rasa-sdk:3.6.2
WORKDIR /app
COPY actions/ /app/actions
# Nếu có requirements cho actions:
# COPY requirements.txt /app/requirements.txt
# RUN pip install -r /app/requirements.txt
EXPOSE 8080
CMD ["start","--actions","actions","--port","8080"]
