tracker_store:
  type: mongo
  url: ${MONGO_URI}
  db: DuAnTotNghiep
  collection: conversations
  login_db: admin

# Action server: dùng biến môi trường để linh hoạt khi deploy.
# Mặc định action server lắng nghe cổng 5055:
#   ACTIONS_URL=http://action-server:5055
action_endpoint:
  url: ${ACTIONS_URL}/webhook
